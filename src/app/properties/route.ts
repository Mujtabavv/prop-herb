import { NextResponse } from 'next/server';

export async function GET() {
  // This is a placeholder API route for future implementation
  return NextResponse.json({ message: 'Properties API route' });
}
